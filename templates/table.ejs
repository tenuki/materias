<% if (DATA.length===0) { %>
    <tr>
        <td class="is-narrow"></td>
        <td class="is-narrow"></td>
        <td>(nada por el momento)</td>
    </tr>
<% } %>
<% DATA.forEach(function(reg){ %>
    <tr>
<!--        <td class="is-narrow"><%= reg.desde %></td>-->
<!--        <td class="is-narrow"><%= reg.hasta %></td>-->
        <td class="p-1 is-narrow <%= reg.classcolor %>"> </td>
        <td class="p-1 is-narrow has-text-centered has-text-weight-bold"><%= reg.desde %> - <%= reg.hasta %></td>

    <% if (show_pabellon) { %>
        <td class="p-1 is-narrow has-text-centered has-text-weight-bold">
        <%= reg.pabellon %>
        </td>
    <% } %>

        <td class="p-1 is-narrow has-text-centered has-text-weight-bold">
            <% if (reg.is_composite) { %>
                Ver<br/>&rarr;
            <% } else { %>
                <%= reg.aula %>
            <% } %>
        </td>

        <td class="p-1">
<!--            <% if (reg.is_composite) { %>-->
<!--                Aulas:-->
<!--                <% reg.components.forEach(function(aula_sub) { %>-->
<!--                     <%= aula_sub.aula %> :  <%= aula_sub.fecha %> <%= aula_sub.turno %> |-->
<!--                <% }) %>-->
<!--                <%= reg.aula %> :  <%= reg.fecha %> <%= reg.turno %>-->
<!--            <% } %>-->

            <table class="p-0">
                <tr class="p-0">
                <td class="is-narrow p-0">

                        <table class="p-0">
                            <% reg.materia.forEach(function(namep) { %>
                                <tr class="p-0">
                                    <td class="p-0">
                                        <%= namep %>
                                    </td>
                                </tr>
                            <% }) %>
                        </table>

                </td>
                                <% if (reg.is_composite) { %>
                    <td class="is-narrow p-0">
                                    <table class="is-narrow p-0">
                                        <tbody class="is-narrow p-0">

                                            <tr class="is-narrow p-0">
                                            <% reg.extra.forEach(function(aula_sub) { %>
                                                <td class="is-narrow p-0 has-text-centered">A:<%= aula_sub[0] %></td>
                                            <% }) %>
                                            </tr>

                                            <tr class="is-narrow p-0">
                                            <% reg.extra.forEach(function(aula_sub) { %>
                                                 <td class="is-narrow p-0 has-text-centered"><%= aula_sub[1] %></td>
                                            <% }) %>
                                            </tr>
                                        </tbody>
                                    </table>
                        </td>
                    <% } %>
                </tr>
            </table>



        </td>
    </tr>
<% }) %>
