<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>FCEN - {% block title %}{% endblock %}</title>
    <link rel="stylesheet" href="/static/bulma-1.0.2.css">
    {% block head %}{% endblock %}

    <script>
        {% block scripts %}{% endblock %}
    </script>
  </head>
  <body class="is-size-3-desktop has-text-weight-medium">


<!-- Main container -->
<nav class="level">
  <!-- Left side -->
  <p class="level-left">
    Horario: {{str(desde)}}<br/>
    <a href="{{data_url[0][1]}}">{{data_url[0][0]}}</a>
  </p>


   <p class="level-item has-text-centered">
        Pabellón {{pabellon}} <br/> Día {{dia}} !
  </p>


  <!-- Right side -->
  <div class="level-right">
    Servidor: {{(str(now())+'.').split('.')[0].split(' ')[1]}} <br/>
     <a href="{{data_url[1][1]}}">{{data_url[1][0]}}</a>
  </div>
</nav>



  {% block content %}{% endblock %}

  </body>
</html>